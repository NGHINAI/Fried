node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# voiceover build artifacts
whisper.cpp
tmp.json
public/*.aiff
# ignore all VO/intermediate wavs (vo.wav, s1.wav, vo_16k.wav…), keep the SFX
public/*.wav
!public/thud.wav
!public/whoosh.wav

# TTS models (310MB+) and Python venv — never commit
tts-models/
.venv-tts/
